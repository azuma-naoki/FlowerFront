export async function fetchData(emotion) {
    // const response = await fetch(
    //   `127.0.0.1:5000/flower/${emotion}`
    // );
    // const data = await response.json();
    // return data.message;
    return 0;
  }